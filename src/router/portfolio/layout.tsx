import { Outlet } from 'react-router-dom';

export default function PortfolioLayout() {
    return (
        <div>
            <span>PortfolioLayout</span>
            <Outlet />
        </div>
    );
}
